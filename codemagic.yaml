workflows:
  default-workflow:
    name: Default Workflow
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.hirokino.namecardapp
    scripts:
      - name: Get Flutter packages
        script: |
          cd namecard_app && flutter pub get
      - name: Build IPA
        script: |
          BUILD_NUM=$(date +%y%m%d%H%M)
          echo "BUILD_NUM=$BUILD_NUM"
          perl -i -pe "s/^version: .*/version: 0.9.0+$BUILD_NUM/" namecard_app/pubspec.yaml
          cat namecard_app/pubspec.yaml | grep version
          cd namecard_app
          flutter build ipa \
            --release \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - namecard_app/build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
